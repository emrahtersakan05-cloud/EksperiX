import { ImageIcon } from "lucide-react";
import PlaceholderPage from "@/components/placeholder-page";

export default function ResimDuzenlemePage() {
  return (
    <PlaceholderPage
      title="Resim Düzenleme"
      description="Rapor görsellerinizi kırpma, boyutlandırma ve işaretleme araçlarıyla düzenleyin."
      icon={ImageIcon}
    />
  );
}
