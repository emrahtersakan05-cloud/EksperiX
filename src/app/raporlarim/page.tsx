import { FileText } from "lucide-react";
import PlaceholderPage from "@/components/placeholder-page";

export default function RaporlarimPage() {
  return (
    <PlaceholderPage
      title="Raporlarım"
      description="Hazırlanan değerleme raporlarınızı buradan inceleyebilirsiniz."
      icon={FileText}
    />
  );
}
